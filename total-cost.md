| eval inputPricePerMTok = case(
    like(modelId, "%opus-4-7%"),            5.00,
    like(modelId, "%opus-4-6%"),            5.00,
    like(modelId, "%opus-4-5%"),            5.00,
    like(modelId, "%sonnet-4-6%"),          3.00,
    like(modelId, "%sonnet-4-5%"),          3.00,
    like(modelId, "%claude-3-5-sonnet%"),   6.00,
    like(modelId, "%claude-3-sonnet%"),     3.00,
    like(modelId, "%haiku-4-5%"),           1.00,
    like(modelId, "%claude-3-5-haiku%"),    1.00,
    like(modelId, "%claude-3-haiku%"),      0.25,
    like(modelId, "%nova-premier%"),        2.00,
    like(modelId, "%nova-pro%"),            0.80,
    like(modelId, "%nova-lite%"),           0.06,
    like(modelId, "%nova-micro%"),          0.035,
    like(modelId, "%titan-text-express%"),  0.20,
    like(modelId, "%titan-text-lite%"),     0.15,
    like(modelId, "%titan-embed%"),         0.10,
    like(modelId, "%llama4%"),              0.72,
    like(modelId, "%llama3.3%"),            0.72,
    like(modelId, "%llama3-70b%"),          0.72,
    like(modelId, "%llama3-8b%"),           0.22,
    like(modelId, "%llama3-1%"),            0.72,
    like(modelId, "%mistral-large%"),       0.50,
    like(modelId, "%mistral-small%"),       0.20,
    like(modelId, "%deepseek%"),            0.62,
    like(modelId, "%minimax%"),             0.30,
    like(modelId, "%gemma%"),               0.04,
    true(),                                 1.00
  )

| eval outputPricePerMTok = case(
    like(modelId, "%opus-4-7%"),            25.00,
    like(modelId, "%opus-4-6%"),            25.00,
    like(modelId, "%opus-4-5%"),            25.00,
    like(modelId, "%sonnet-4-6%"),          15.00,
    like(modelId, "%sonnet-4-5%"),          15.00,
    like(modelId, "%claude-3-5-sonnet%"),   30.00,
    like(modelId, "%claude-3-sonnet%"),     15.00,
    like(modelId, "%haiku-4-5%"),           5.00,
    like(modelId, "%claude-3-5-haiku%"),    5.00,
    like(modelId, "%claude-3-haiku%"),      1.25,
    like(modelId, "%nova-premier%"),        8.00,
    like(modelId, "%nova-pro%"),            3.20,
    like(modelId, "%nova-lite%"),           0.24,
    like(modelId, "%nova-micro%"),          0.14,
    like(modelId, "%titan-text-express%"),  0.60,
    like(modelId, "%titan-text-lite%"),     0.20,
    like(modelId, "%titan-embed%"),         0.00,
    like(modelId, "%llama4%"),              0.72,
    like(modelId, "%llama3.3%"),            0.72,
    like(modelId, "%llama3-70b%"),          0.72,
    like(modelId, "%llama3-8b%"),           0.22,
    like(modelId, "%llama3-1%"),            0.72,
    like(modelId, "%mistral-large%"),       1.50,
    like(modelId, "%mistral-small%"),       0.60,
    like(modelId, "%deepseek%"),            1.85,
    like(modelId, "%minimax%"),             1.20,
    like(modelId, "%gemma%"),               0.08,
    true(),                                 5.00
  )
